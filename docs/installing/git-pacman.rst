To install :program:`git`, run this :program:`pacman` command (as ``root``):

.. code-block:: shell

   pacman --sync --noconfirm git

or equivalently:

.. code-block:: shell

   pacman -S --noconfirm git
