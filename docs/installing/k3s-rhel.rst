For this family of operating systems, Rancher recommends [#fn2]_ issuing the following command before installing K3s (as ``root`` or with :program:`sudo`):

.. code-block:: shell

   sudo systemctl disable --now firewalld
