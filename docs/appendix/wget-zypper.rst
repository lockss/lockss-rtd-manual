To install :program:`wget`, run these :program:`zypper` (as ``root``):

.. code-block:: shell

   zypper refresh

   zypper --non-interactive install wget

or equivalently:

.. code-block:: shell

   zypper refresh

   zypper -n install wget
